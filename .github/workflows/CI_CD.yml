name: CI/CD counter app

on:
  push:
    branches: [ main ]

jobs:
#First we will see the application build or not , then we will deploy in EC2
  #build:
   # runs-on: ubuntu-latest
    

  Deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2 
      - name: Deploy in EC2
        env:
            PRIVATE_KEY: ${{ secrets.AWS_PRIVATE_KEY  }}
            HOSTNAME : ${{ secrets.HOSTNAME  }}
            USER_NAME : ${{ secrets.USER_NAME  }}
            
       
            
            
        run: |
        
        
           
           ssh -o StrictHostKeyChecking=no  $PRIVATE_KEY ${USER_NAME}@${HOSTNAME} '
           
           git clone https://avinashkhadgi30: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@github.com/avinashkhadgi30/Test.git
           '


           
